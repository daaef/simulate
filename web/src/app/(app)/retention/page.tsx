"use client";

export default function RetentionPage() {
  return (
    <div style={{ padding: "24px" }}>
      <h1 style={{ margin: "0 0 8px", fontSize: "36px", color: "var(--text-primary)" }}>Retention</h1>
      <p style={{ margin: 0, color: "var(--text-secondary)" }}>
        Retention governance and archive lifecycle controls are queued after archive metadata support lands.
      </p>
    </div>
  );
}
