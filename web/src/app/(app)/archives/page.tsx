"use client";

export default function ArchivesPage() {
  return (
    <div style={{ padding: "24px" }}>
      <h1 style={{ margin: "0 0 8px", fontSize: "36px", color: "var(--text-primary)" }}>Archives</h1>
      <p style={{ margin: 0, color: "var(--text-secondary)" }}>
        Archive browsing is planned after the runs workspace and execution snapshots are split properly.
      </p>
    </div>
  );
}
